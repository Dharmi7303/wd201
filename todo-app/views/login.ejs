<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>
    <%= title %>
  </title>
  <meta name="csrf-token" content=" <%=csrfToken%> " />
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
  <div class="grid grid-cols-6">
    <div class="col-span-2 col-start-3">
      <%- include('header.ejs') %>
        <h6 class="py-4">Login to your account</h6>
        <form action="/session" method="post">
          <input type="hidden" name="_csrf" value="<%=csrfToken%>" />

          <div class="py-3">
            <label for="email">Email</label>
            <input class="border border-gray-300 rounded p-2 w-full text-gray-900" type="email" name="email" />
          </div>

          <div class="py-3">
            <label for="password">Password</label>
            <input class="border border-gray-300 rounded p-2 w-full text-gray-900" type="password" name="password" />
          </div>

          <div class="py-3">
            <button class="text-green-600 rounded text-white bg-green-600 hover:bg-green-800 font-medium px-5 py-2"
              type="submit">
              Sign in
            </button>
          </div>
        </form>
        <% if(messages && messages.error && messages.error.length> 0) { %>
          <% for(var i=0; i<messages.error.length; i++) { %>
            <li  class="text-red-500" >
              <%= messages.error[i] %>
            </li>
            <% } %>
              <% } %>
    </div>
  </div>
</body>

</html>